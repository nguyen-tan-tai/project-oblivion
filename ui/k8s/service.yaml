apiVersion: v1
kind: Service
metadata:
  name: oblivion-ui
  labels:
    app: oblivion-ui
spec:
  selector:
    app: oblivion-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
  type: NodePort